name: protobuf
version: 2.2.0

authors:
  - Jerome Gravel-Niquet <jeromegn@gmail.com>

scripts:
  postinstall: shards build && mkdir -p ../../bin && cp bin/protoc-gen-crystal ../../bin

executables:
  - protoc-gen-crystal

targets:
  protoc-gen-crystal:
    main: bin/protoc-gen-crystal.cr

license: MIT
